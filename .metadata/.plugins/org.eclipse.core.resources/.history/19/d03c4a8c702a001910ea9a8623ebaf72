#include 'protheus.ch'
#include 'parmtype.ch'

user function ESTRREP()
	/*
	Local nCount
	Local nNum := 0
	
	//For nCount := 0 To 10
	For nCount := 0 To 10 Step 2 //de 2 em 2
	nNum += nCount
	Next
	Alert("Valor: "+cValToChar(nNum))
	*/
	
	
	
return